#pragma once

template <typename T>
T max(T a, T b) {
    return (a > b) ? a : b;
}

template <typename T>
T square(T x) {
    return x * x;
}

template<typename T>
T product1(T a, T b) {
    return a * b;
}
